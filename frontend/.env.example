# Backend API Base URL Configuration
# For production or custom deployment, set VITE_API_BASE_URL to your backend API endpoint URL
# Default (if unassigned): Dynamically resolves to http://<hostname>:8000/api
VITE_API_BASE_URL=http://localhost:8000/api
